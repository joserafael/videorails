<%= form_with(model: video, local: true, html: { multipart: true }) do |form| %>
  <% if video.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(video.errors.count, "error") %> prohibited this video from being saved:</h2>

      <ul>
      <% video.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="col-md-6 col-lg-6 col-sm-12">
  <div class="form-group">
    <%= form.label :titulo %>
    <%= form.text_field :titulo, id: :video_titulo, :class=> 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion, id: :video_descripcion, :class=> 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :archivovideo %>
    <%= form.file_field :archivovideo, id: :video_archivovideo, :class=> 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.submit %>
  </div>
<% end %>

</div>